import { EntitySchema, PropertyManifest, PropertySchema } from '@repo/types';
export declare class PropertyManifestService {
    transformPropertyManifest(propSchema: PropertySchema, entitySchema?: EntitySchema): PropertyManifest;
}
